# 删除图片上传和图片识别功能的计划

## 需要删除的文件：
1. **FileUploadController.java** - 整个文件上传控制器（包含图片上传）
2. **ImageOcrService.java** - 图片 OCR 识别服务

## 需要修改的文件：

### 后端 Java 代码：
3. **FileParserService.java** - 删除 ImageOcrService 依赖，移除 image 类型支持
4. **AppConfig.java** - 删除 OCR 相关配置字段和方法
5. **ContentRequest.java** - 删除 fileType 字段中的 image 类型说明

### 前端代码：
6. **index.html** - 删除文件上传相关 UI，只保留文本输入
7. **main.js** - 删除文件上传、文件类型判断、文件解析相关代码

### 配置文件：
8. **application.properties** - 删除 OCR 配置项和图片文件类型限制

### Maven 依赖：
9. **pom.xml** - 删除 Tesseract OCR (tess4j) 依赖

## 修改后的功能：
- 保留文本输入功能
- 保留 Word 和 PDF 文件解析（如果需要）
- 保留 AI 内容分析功能
- 保留 HTML 生成功能
- 移除所有图片上传和 OCR 识别功能